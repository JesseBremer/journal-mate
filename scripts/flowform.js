function handleButtonClick(buttonNumber) {
    let overview = [];
    // Overview array will be the holder of every button's input. This will be then accessed and displayed in an undertermined 
    // format. Ideally inputs will be grouped together (button 1 with button 1 inputs, 2 with 2 ect.)

    //When a button is clicked the page only displays the following:
    //a question <paragraph tag> and the user entry text area,and a save button.
    //Save button will add to the overview array for future.

  }

  