function preferenceChoice(event) {
    if (event === 1) {
    window.location.href = 'freeform.html';
    } else if (event === 2) {
        console.log("flowform");
    } else if (event === 3) {
        console.log('rigidform')
    }
}